import fs from "fs"; const ts = Date.now(); const data = JSON.parse(fs.readFileSync("./symbolic_memory/trainer_learned.json", "utf-8")); const keyword = "energy"; let hits = []; for (const entry of data) { if (entry.context?.toLowerCase().includes(keyword.toLowerCase())) { hits.push(entry.symbol); } } console.log(`🔍 Found ${hits.length} matches for "${keyword}"`); console.log(`⏱️ Search completed in ${Date.now() - ts} ms`); console.log("📌 Symbols:", hits.slice(0, 10), "...");
