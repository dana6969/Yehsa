import fs from "fs"; import { receiveMeshCommand } from "./symbolic/mesh_listener.js"; import { getMappedAction } from "./symbolic/symbolic_dictionary.js"; const dict = JSON.parse(fs.readFileSync("symbolic_memory/full_dictionary.json", "utf-8")); const symbols = Object.keys(dict).slice(0, 1000); const start = Date.now(); for (const symbol of symbols) { const action = getMappedAction(symbol); receiveMeshCommand({ symbol, action }); } const elapsed = Date.now() - start; console.log(`✅ Executed ${symbols.length} symbols in ${elapsed} ms (${(elapsed / 1000).toFixed(2)}s)`);
