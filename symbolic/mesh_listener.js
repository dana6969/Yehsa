import { executeSymbol } from "./symbolic_action_executor.js"; export function receiveMeshCommand({ symbol, action }) { console.log(`📥 MESH RX] Received: ${symbol} → ${action}`); console.log(`⚙️ Executing: ${symbol}`); executeSymbol(symbol, action); }
